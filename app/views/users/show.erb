<h1><%= @user.name %>'s Profile Page</h1>

<p><%= image_tag @user.profile_img, class: "usr-img" %></p>

<% @review.each do |r| %>
<ol>
  <li><%= link_to r.coffee.title, user_coffee_path(r.id) %></li>
  <br>
  <%= link_to r.title, review_path(r.id) %>
  <p>Rating: <%= r.rating %></p>
  <p><%= r.description %></p>
</ol>
<% end %>
